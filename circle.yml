
version: 2.1
orbs:
  # not specifying a version installs the latest version.
  # specify a version of the orb with: cypress-io/cypress@x.x.x
  cypress: cypress-io/cypress
workflows:
  build:
    jobs:
      - cypress/run # "run" job comes from "cypress" orb
