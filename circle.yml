
version: 2.1
orbs:
  cypress: cypress-io/cypress@1 # installs the latest version of our cypress 1.x.x orb
workflows:
  build:
    jobs:
      - cypress/run: # "run" job comes from "cypress" orb
          start: 'npm start' # start server before running tests
